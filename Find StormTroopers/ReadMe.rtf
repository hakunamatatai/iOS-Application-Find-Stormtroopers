ReadMe

1. This application should be simulated on iPhone device because this application will use back camera of device. 
Simulation on simulator will not work!

2. Add library SWRealViewController. Select Find Stormtrooper project, 
click mouse on the right button and then select “New Group”. Set it name as “Menu”. 
Then add two files into “Menu” folder.
Xcode will generate a file named “Find Stormtrooper-Bridging-Header.h”, open this file and add the following code into it.


